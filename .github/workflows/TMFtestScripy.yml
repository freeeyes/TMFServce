name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: |
        cd ./TimerMessageFrame/Bin/
        ls
        make
        ./TMService
